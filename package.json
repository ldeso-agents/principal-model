{
  "name": "principal-model",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Phase B simulator and Observable Framework report for the Klima fee vs. principal research note.",
  "scripts": {
    "dev": "observable preview",
    "build": "observable build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@observablehq/framework": "^1.13.0",
    "@types/node": "^20.11.0",
    "markdown-it-katex": "^2.0.3",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "vitest": "^1.5.0"
  }
}
